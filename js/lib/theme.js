// ЕДИНАЯ точка истины по всем визуальным значениям интерфейса — цвета, прозрачности границ.
// Больше НИГДЕ в проекте не должно быть hex/rgba-литералов темы прямо в коде — только ссылки
// на THEME.*. Хочешь поменять яркость/цвет/толщину чего-либо во всём интерфейсе разом — меняешь
// здесь, в одном месте, и это применяется во всех трёх нодах (PromptBlocks/Scene Builder/LoRA
// Guide) сразу, без похода по файлам.
//
// Фон ноды снаружи (тело самой ноды в ComfyUI/LiteGraph) сюда НЕ входит — это тема самого ComfyUI,
// мы её не задаём и не можем: см. surface.card ниже — специально светлее корпуса ноды, чтобы не
// сливаться с ним.
export const THEME = {
  // Базовые поверхности — тёмный фон разных уровней вложенности, от каркаса к содержимому.
  surface: {
    card: "#3c4048", // фон карточки блока (референс/стиль/шот/LoRA/персонаж) — было #25272c, +10% лайтнес
    nested: "#33373e", // фон вложенной строки (пункт шота, вещь одежды в Scene Builder) — было #1c1e22, +10%
    button: "#42454f", // фон обычной кнопки — было #2b2d33, +10%
    pillOff: "#484d57", // фон невыбранной пилюли (segmented/chipToggle) — было #31343b, +10%
    smallBtn: "rgba(255,255,255,.05)", // фон маленькой иконки-кнопки (↑ ↓ ✕ и т.п.)
    input: "#121416", // фон поля ввода — самый тёмный уровень, НЕ поднимается вместе с остальными
  },

  border: {
    card: "rgba(255,255,255,.15)", // было .05, +10 пунктов прозрачности — обводка карточки заметнее
    nested: "rgba(255,255,255,.04)",
    button: "rgba(255,255,255,.07)",
    input: "rgba(255,255,255,.08)",
    dashed: "rgba(255,255,255,.15)", // пунктирная рамка (звёздочка-инпут у LoRA-меток и т.п.)
    dashedStrong: "rgba(255,255,255,.35)", // пунктирная рамка «без цвета» в выборе цвета шаблона
  },

  text: {
    title: "#eceef1", // заголовок карточки
    button: "#dcdee3", // текст обычной кнопки
    input: "#e7e8ec", // текст внутри полей ввода
    itemLabel: "#d6d8dc", // название пункта внутри шота
    summary: "#8a8d94", // резюме в шапке карточки (справа) / стрелка сворачивания / инфо-иконка LoRA
    muted: "#9a9ca3", // иконка-кнопка (↑ ↓ ✕), нейтральные serые иконки вложенных строк
    mutedDim: "#75787f", // стрелка сворачивания у строки внутри шота
    faint: "#6b6e76", // самый тусклый текст-подсказка («Добавить описание» и т.п.)
    faintAlt: "#999", // текст+рамка пунктирной кнопки «+ Добавить в шот»
    pillOff: "#c7c9cf", // текст невыбранной пилюли
    pillOn: "#fff", // текст выбранной пилюли
    outlineOn: "#eceef1", // обводка выбранного цвета в свотч-пикере шаблона
  },

  accent: "#4d7bf3", // акцент — выбранная пилюля, совпадает с category.ref

  // Цвет полоски+иконки по типу блока — категория видна с первого взгляда, не читая текст.
  category: {
    ref: "#4d7bf3", style: "#e0a53f", shot: "#4bbf8a", sound: "#b47ecf", music: "#cf7ea0", lora: "#8a7cf0",
  },
  // Полоска карточки без категории/своего цвета — практически никогда не должно случаться
  // (каждый вызывающий card() передаёт opts.color), но на всякий случай.
  stripeFallback: "#666",

  // Общий набор для пользовательских шаблонов (свой цвет у сохранённого стиля/звука/музыки) и
  // меток режима LoRA (цвет по хэшу текста метки).
  palette: ["#4d7bf3", "#e0a53f", "#4bbf8a", "#b47ecf", "#cf7ea0", "#3ddc84", "#e08a8a", "#6ea8d8"],

  // Единственное место с семантическим цветом состояния (вкл/выкл), а не категории.
  status: {
    onIcon: "#3ddc84", // гаечка LoRA — включена (сочно-зелёная)
    onText: "#4bbf8a", // текст/фон пилюли «Вкл» (мягче)
    onBg: "rgba(75,191,138,.16)",
    offIcon: "#6b6e76", // гаечка LoRA — выключена (серая) = text.faint
    offText: "#e08a8a", // текст пилюли «Выкл» / текст кнопки «Сбросить всё»
    offBg: "rgba(224,138,138,.14)",
    resetBg: "rgba(224,138,138,.08)",
    resetBorder: "rgba(224,138,138,.3)",
    bookmark: "#e0a53f", // закрашенная звёздочка ★ = category.style
  },

  // Пилюля-тег для вставки (<Subject 1> и т.п.) — акцентная обводка вместо заливки.
  tag: {
    bg: "rgba(77,123,243,.12)",
    border: "rgba(77,123,243,.45)",
    text: "#a9bdf8",
  },

  // Высоты кнопок — ЕДИНАЯ точка истины (px). Никаких магических чисел высоты по файлам,
  // только ссылки на THEME.heights.*. Группы:
  //  - addBlock/copy/template/resetAll — 4 кнопки главной панели promptBlocks.js, каждая отдельно;
  //  - back — кнопка «← Назад» в меню (lib/menu.js);
  //  - listRow — обычный пункт списка меню (категории верхнего уровня вроде «Визуальный стиль»/
  //    «Шот», и пункты внутри категории вроде «Персонаж»/«Образ» — один и тот же тип строки,
  //    один размер на всех, см. Menu#row в lib/menu.js);
  //  - newTemplate — строка «+ Новый ... (вручную)» в начале списка шаблонов категории;
  //  - templateRow — строка сохранённого шаблона в списке (lib/templates.js templateEntries).
  heights: {
    addBlock: 30,
    copy: 30,
    template: 30,
    resetAll: 30,
    back: 30,
    listRow: 30,
    newTemplate: 30,
    templateRow: 30,
  },
};
